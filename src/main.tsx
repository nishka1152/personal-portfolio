import React from "react";
import ReactDOM from "react-dom/client";
import ScrollAreaDemo from "./App.tsx";
import './index.css';

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <ScrollAreaDemo />
  </React.StrictMode>,
);
